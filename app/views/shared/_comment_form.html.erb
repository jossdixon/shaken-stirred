<%= simple_form_for([cocktail, comment], data: {insert_in_list_target: "form"}, action: "submit->insert-in-list#send") do |f| %>
  <%= f.input :content, as: :text, :label => false %>
  <%= f.button :submit %>
<% end %>
